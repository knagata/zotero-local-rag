# Citation-Update ヘルプガイド

`Citation-Update.command`（Mac）/ `Citation-Update.bat`（Windows）の詳細な使い方と、
ステータスの意味・エラー回復の仕組みをまとめたドキュメントです。

---

## 1. 何をするツールか

Zotero の各文献について、2つのステップを実行して引用ネットワーク DB（`data/relations.db`）を構築します。

| ステップ | 内容 | データ源 |
|---|---|---|
| **Step 1: 被引用取得** | 「この文献は外部のどの論文から引用されているか」を取得し、引用文脈を本文チャンクと照合 | Semantic Scholar API |
| **Step 2: 参照抽出** | EPUB の脚注・章末注から「この文献は何を引用しているか」を抽出し、S2 で文献を解決 | ローカル EPUB + Semantic Scholar API |

Step 1 の前処理として、DOI 未設定の文献は OpenAlex でタイトルから DOI を解決し、
`.env` に `ZOTERO_USER_ID` + `ZOTERO_API_KEY` があれば Zotero ライブラリへ自動書き戻しします。

---

## 2. メニューの選び方

```
1) Update a specific item by ID      … 特定の1件を更新（処理済みならスキップ）
2) Force re-update a specific item   … 特定の1件を強制再処理
3) Update ALL items                  … 全件更新（処理済みはスキップ）★通常はこれ
4) Force re-update ALL items         … 全件強制再処理（長時間）
5) Resume skipped EPUB refs          … バジェット超過で保留した EPUB 参照を追加解決
```

### どれを使えばいいか

| やりたいこと | メニュー |
|---|---|
| 新しく追加した文献の引用情報を取りたい | **3** |
| 前回実行でエラーになった分を回収したい | **3**（エラー分だけ自動で再処理される） |
| EPUB の参照ネットワークをもっと充実させたい | **5** |
| 特定の文献だけ更新したい | **1**（やり直しは **2**） |
| DB を一から作り直したい | **4**（数時間〜十数時間かかる） |

> **ポイント**: エラー回収のために 4（Force）を使う必要はありません。
> 失敗したアイテム・参照は `error` ステータスとして記録されており、
> **3 を実行するだけで該当分のみ再処理**されます。

### メニュー 3 の処理内容（forceなし）

1. ステータスが `mapped`（完了済み）のアイテムはスキップ（DOI/ISBN のメタデータ同期のみ）
2. `error` / `pending` / 未処理のアイテムを処理
3. 最後に、EPUB 参照のうち `s2_status='error'` の行を自動で再試行

### メニュー 5（Resume skipped）の目的

脚注が大量にある書籍（編著など）では、S2 への問い合わせがアイテムあたり 50 件
（`--epub-budget` で変更可）に制限され、超過分は `skipped` として DB に保留されます。
メニュー 5 はこの保留分を、指定したバジェット（デフォルト 200/アイテム）で追加解決します。
EPUB の再解析は不要で、DB の行を直接更新します。`error` 行も対象に含まれます。

**これはエラー回復用ではなく、参照ネットワークを「もっと深く」したいときに使う機能です。**
保留行数が多いとかなり時間がかかるため、必要になったときに実行すれば十分です。

---

## 3. ステータスの意味

### アイテム単位（`item_citation_status.s2_status`）

| 値 | 意味 | 次回メニュー3での扱い |
|---|---|---|
| `mapped` | 両ステップ完了 | スキップ |
| `s2_done` | Step 1 のみ完了 | Step 2（EPUB）のみ実行 |
| `pending` | 処理中に中断された | 全ステップ再処理 |
| `error` | S2 障害などで失敗 | 全ステップ再処理 |

### EPUB 参照単位（`global_references.s2_status`）

| 値 | 意味 | 再試行されるか |
|---|---|---|
| `matched` | S2 解決済み + 本文チャンク照合済み | — |
| `mapped` | S2 解決済み（チャンク照合なし） | — |
| `no_context` | S2 で発見したが引用文脈なし | — |
| `no_chunk` | 文脈はあるがチャンク照合失敗 | — |
| `not_found` | S2 に該当文献なし（正常終了） | されない |
| `skipped` | バジェット超過で S2 未問い合わせ | メニュー 5 |
| `error` | S2 障害（429 等）で失敗 | メニュー 3 の終盤 / メニュー 5 |

---

## 4. レート制限と 429 エラーの仕組み

Semantic Scholar API は、API キーがあっても公称制限（1 req/sec）内で
429 Too Many Requests を返すことがあります（サーバー負荷による既知の問題）。
本ツールは以下の戦略で対処します。

- **リクエスト間隔**: API キーあり 2.5 秒 / なし 3.5 秒（プロセス間で共有・`data/s2_rate.lock`）
- **429 時のリトライ**: 最大 3 回、指数バックオフ + ジッター（上限 45 秒）。
  `Retry-After` ヘッダがあればそれに従う
- **サーキットブレーカー**: 3 リクエスト連続でリトライ枯渇した場合、
  「S2 が落ちている」と判断して **5 分間 S2 への問い合わせをスキップ**します。
  その間も EPUB 抽出など他の処理は全速で継続し、スキップ分は `error` として記録されます。
  5 分後に自動で再開を試みます

この設計により、S2 が不調でも実行全体がハングすることはなく、
取りこぼしは後から（同じ実行の終盤、または次回のメニュー 3 で）自動回収されます。

### ログの読み方

| ログ表示 | 意味 | 対応 |
|---|---|---|
| `[S2] 429 Too Many Requests (attempt 1/3), waiting Ns...` | S2 が混雑中。自動リトライ中 | 不要 |
| `[S2] Gave up after 3 retries: <URL>` | リトライ枯渇。`error` として記録 | 不要（次回メニュー 3 で自動回収） |
| `[S2] ... circuit breaker open: skipping S2 lookups for 300s` | ブレーカー発動。5 分間 S2 をスキップ | 不要 |
| `[WARN] S2 step failed for <KEY>; status set to 'error'` | アイテム単位の失敗を記録 | 不要（次回メニュー 3 で再処理） |

---

## 5. 環境変数（`.env`）

| 変数 | 効果 |
|---|---|
| `S2_API_KEY` | S2 のリクエスト間隔が 3.5 → 2.5 秒に短縮 |
| `ZOTERO_USER_ID` + `ZOTERO_API_KEY` | OpenAlex で解決した DOI を Zotero ライブラリへ自動書き戻し |

Zotero Web API キーは https://www.zotero.org/settings/keys で発行できます
（書き込み権限が必要）。`ZOTERO_USER_ID` は同ページに表示される数値 ID です。

---

## 6. よくある質問

**Q. 前回の実行でエラーが出ていた。Force rebuild（メニュー 4）が必要？**
A. 不要です。エラーは `error` ステータスで記録されているので、メニュー 3 で該当分だけ再処理されます。

**Q. メニュー 3 の所要時間は？**
A. スキップされるアイテムは一瞬です。実処理はエラー/未処理アイテム数 × 数分（被引用数・脚注数による）。

**Q. `not_found` が多いのは異常？**
A. 正常です。書籍・人文系文献・非英語文献は S2 に収録されていないことが多く、
これらは「調べた結果なかった」という確定情報として記録されます。

**Q. 日本語の文献はどう扱われる？**
A. S2 / OpenAlex は英語文献中心のため、タイトルの非 ASCII 比率が 30% を超える場合は
検索せずスキップします（無駄なリクエストと誤マッチの防止）。

**Q. 処理を途中で止めたらどうなる？**
A. 処理中のアイテムは `pending` のまま残り、次回メニュー 3 で再処理されます。DB が壊れることはありません。

**Q. Zotero から削除した文献のデータは？**
A. `Library-Update`（インデクサー）実行時に、削除済みアイテムの引用データも自動でパージされます。
