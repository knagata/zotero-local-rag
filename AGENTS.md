# Project instructions

Codex とサブエージェントは、作業開始時にリポジトリルートの `CLAUDE.md` を最初から最後まで
必ず読むこと。正本、開発、テスト、検証、記録の規則はそちらに従い、このファイルへ複製しない。

`CLAUDE.md`を読んだ後、`memory/projects/current-development.md`が存在すれば現在のセッション
引継ぎとして読むこと。完了・方針変更・測定値更新時は、コードと同じコミットで更新する。
